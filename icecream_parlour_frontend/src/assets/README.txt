This folder is used to reference the provided static CSS files from the project root /assets directory.

The component imports:
- ../../assets/common.css
- ../../assets/macbook-pro-16-inch-right-view-mockup-label-60-194.css

Image assets are referenced using the absolute path /assets/figmaimages/... so they resolve when the app runs.
